{
  "name": "realtime-chat",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev:frontend": "pnpm --prefix frontend dev",
    "dev:backend": "cd backend && bundle exec rails s",
    "dev": "concurrently --names \"FRONT,BACK\" -c \"bgGreen.bold,bgBlue.bold\" \"npm:dev:frontend\" \"npm:dev:backend\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "commit": "npx git-cz",
    "fix:frontend": "cd frontend && pnpm biome check --write --unsafe .",
    "fix:backend": "cd backend && bundle exec rubocop -A",
    "fix:all": "pnpm fix:frontend && pnpm fix:backend",
    "check:frontend": "cd frontend && pnpm biome check . && pnpm type-check && pnpm test:unit --run",
    "check:backend": "cd backend && bundle exec rubocop && bundle exec rails test",
    "check:all": "pnpm check:frontend && pnpm check:backend",
    "prepare": "husky"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "git-cz": "^4.9.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "typescript": "^7.0.2"
  },
  "lint-staged": {
    "frontend/**/*.{js,ts,vue,json,css}": [
      "bash -c 'cd frontend && ./node_modules/.bin/biome check --write --unsafe \"$@\"' _"
    ],
    "backend/**/*.rb": [
      "bash -c 'cd backend && bundle exec rubocop -A --force-exclusion \"$@\"' _"
    ]
  }
}
